"use client";

import { PortalPlaceholderPage } from "@/components/portal/PortalPlaceholderPage";

export default function VaultMessagesPage() {
  return (
    <PortalPlaceholderPage
      role="vault"
      title="Messages"
      description="Operational threads attach to each file. Open Pipeline or a file workspace to review conversation history."
    />
  );
}
