"use client";

import { PortalPlaceholderPage } from "@/components/portal/PortalPlaceholderPage";

export default function EscrowDocumentsPage() {
  return (
    <PortalPlaceholderPage
      role="escrow"
      title="Document intake"
      description="Closing package intake lives on each file workspace. Use the closing queue to open a file and review documents."
    />
  );
}
