"use client";

import { PortalPlaceholderPage } from "@/components/portal/PortalPlaceholderPage";

export default function EscrowMessagesPage() {
  return (
    <PortalPlaceholderPage
      role="escrow"
      title="Messages"
      description="Closing threads attach to each file. Open the closing queue to coordinate on a file workspace."
    />
  );
}
