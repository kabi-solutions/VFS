"use client";

import { PortalPlaceholderPage } from "@/components/portal/PortalPlaceholderPage";

export default function LenderMessagesPage() {
  return (
    <PortalPlaceholderPage
      role="lender"
      title="Messages"
      description="Messaging attaches to each file. Open the underwriting queue to work conditions and notes on a file."
    />
  );
}
